# Base Configuration
base.url=https://studyleo.com/en/universities

# Timeouts (seconds)
timeout.explicit=15
timeout.page.load=45
timeout.implicit=3

# Browser Settings
headless.mode=false

# Retry Settings
retry.count=2

# Wait Times (milliseconds)
wait.short=500
wait.medium=1000
wait.long=2000